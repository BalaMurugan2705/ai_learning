diff --git a/app/chunker.py b/app/chunker.py
index ad81e34..bbf7330 100644
--- a/app/chunker.py
+++ b/app/chunker.py
@@ -1,6 +1,3 @@
-from app.loader import load_document
-
-
 def simple_chunk(text, chunk_size=300):
     chunks = []
 
@@ -24,6 +21,18 @@ def structure_aware_chunk(
 
     inside_code_fence = False
 
+    # Tracks the nearest H1/H2/H3 headings seen so far, so each chunk can be
+    # tagged with the section it falls under (e.g. "Client.send() → Parameters").
+    headers = {1: None, 2: None, 3: None}
+
+    def current_section():
+        parts = [headers[level] for level in (2, 3) if headers[level]]
+        if parts:
+            return " → ".join(parts)
+        return headers[1] or ""
+
+    section_at_chunk_start = current_section()
+
     for line in lines:
 
         is_code_fence = line.strip().startswith("```")
@@ -38,17 +47,42 @@ def structure_aware_chunk(
         if is_header and not inside_code_fence:
 
             if current_chunk:
-                chunks.append("\n".join(current_chunk))
+                chunks.append(
+                    (
+                        "\n".join(current_chunk),
+                        section_at_chunk_start,
+                    )
+                )
                 current_chunk = []
 
+            level = len(line) - len(line.lstrip("#"))
+            heading_text = line[level:].strip()
+
+            if level == 1:
+                headers[1] = heading_text
+                headers[2] = None
+                headers[3] = None
+            elif level == 2:
+                headers[2] = heading_text
+                headers[3] = None
+            else:
+                headers[3] = heading_text
+
+            section_at_chunk_start = current_section()
+
         current_chunk.append(line)
 
     if current_chunk:
-        chunks.append("\n".join(current_chunk))
+        chunks.append(
+            (
+                "\n".join(current_chunk),
+                section_at_chunk_start,
+            )
+        )
 
     result = []
 
-    for chunk_number, chunk_text in enumerate(chunks):
+    for chunk_number, (chunk_text, section) in enumerate(chunks):
 
         chunk = create_chunk(
             text=chunk_text,
@@ -57,13 +91,14 @@ def structure_aware_chunk(
             sdk_version=sdk_version,
             page_type=page_type,
             chunk_number=chunk_number,
+            section=section,
         )
 
         result.append(chunk)
 
     return result
 
-def create_chunk(text, source_file, page_id, sdk_version, page_type, chunk_number):
+def create_chunk(text, source_file, page_id, sdk_version, page_type, chunk_number, section=""):
     return {
         "chunk_id": f"{page_id}-{sdk_version}-{chunk_number}",
         "text": text,
@@ -72,5 +107,6 @@ def create_chunk(text, source_file, page_id, sdk_version, page_type, chunk_numbe
             "page_id": page_id,
             "sdk_version": sdk_version,
             "page_type": page_type,
+            "section": section,
         },
     }
\ No newline at end of file
diff --git a/app/generator.py b/app/generator.py
index e5c0470..57c1b34 100644
--- a/app/generator.py
+++ b/app/generator.py
@@ -1,10 +1,13 @@
 import os
+from pathlib import Path
 
 from groq import Groq
 
 from dotenv import load_dotenv
 
-load_dotenv()
+# Load app/.env explicitly: a bare load_dotenv() searches upward from the
+# working directory, so it misses this file when uvicorn runs from the repo root.
+load_dotenv(Path(__file__).with_name(".env"))
 
 client = Groq(
     api_key=os.getenv("GROQ_API_KEY")
diff --git a/app/rag.py b/app/rag.py
index 0fa438c..761d3e6 100644
--- a/app/rag.py
+++ b/app/rag.py
@@ -40,6 +40,9 @@ def answer_question(question, sdk_version=None):
     # 5. Get similarity distances.
     distances = results["distances"][0]
 
+    # 5b. Get chunk ids, so citations can point at a real chunk.
+    chunk_ids = results["ids"][0]
+
     # 6. The first result is the closest result.
     best_distance = distances[0]
 
@@ -77,12 +80,14 @@ SECTION: {metadata.get('section', '')}
         context=context,
     )
 
-    # 11. Build source information.
+    # 11. Build source information. chunk_id comes straight from Chroma's own
+    # ids, so every citation resolves to a real, retrievable chunk.
     sources = []
 
-    for metadata in metadatas:
+    for chunk_id, metadata in zip(chunk_ids, metadatas):
 
         source = {
+            "chunk_id": chunk_id,
             "source_file": metadata["source_file"],
             "page_id": metadata["page_id"],
             "section": metadata.get("section", ""),
diff --git a/app/vector_store.py b/app/vector_store.py
index e8119af..2886162 100644
--- a/app/vector_store.py
+++ b/app/vector_store.py
@@ -24,7 +24,7 @@ def add_chunks(collection, chunks, embeddings):
         documents.append(chunk["text"])
         metadatas.append(chunk["metadata"])
 
-    collection.add(
+    collection.upsert(
         ids=ids,
         documents=documents,
         embeddings=embeddings,
